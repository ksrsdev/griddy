ctx.data.stateData not layout

stateData contains layout AND state variables (intro startTime etc)

normal loop is: Input -> Logic(states/ai) -> Physics -> Render


//Resource Library
typedef struct {
    char key[64];         // e.g., "intro_title"
    SDL_Texture *texture;
} ResourceEntry;

typedef struct {
    ResourceEntry entries[MAX_RESOURCES];
    int count;
} ResourceManager;

typedef struct {
    SDL_Renderer *renderer;
    ResourceManager cache; // The library
} GameEngine;
