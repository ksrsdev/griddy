Ok so I can get the "accurate" screen size via SDL_GetWindowSize() 
I can use that to calculate UI layouts just like in raylib.
Will need to manually load fonts of either

Ok so basically you're going to do a manual letterbox for your given window size.
You will need to perform matrix multiplication (transformation) to figure out where to render something from the game onto the actual window and vice versa (where the mouse is on the game relative to the window)

WorldToScreen(vec2 virtualCoords) -> vec2 pixelCoords

ScreenToWorld(vec2 pixelCoords) -> vec2 virtualCoords

1) Write the letterbox function
2) Get Mouse Position RELATIVE TO THE GAME SCREEN!
3) Draw something originally relative to the game screen on the window (say click and then there pops a rectangle)

Your UI will be designed over a "master" size (1920x1080) and just simply scale
	>The "best" solution is to use a clamping / responsive UI system but IDK if I have that in me
